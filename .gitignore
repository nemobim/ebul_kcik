# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
*.tsbuildinfo

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env

# 서비스 계정 키 (실제 비밀). 레포 밖에 보관하는 것이 원칙이지만,
# 실수로 프로젝트 안에 내려받아도 커밋되지 않도록 방어적으로 제외한다.
*-firebase-adminsdk-*.json
serviceAccount*.json
service-account*.json
firebase-key*.json
.firebase-credentials/

# Firebase CLI 로컬 캐시
.firebase/

# Claude Code 로컬 설정과 에이전트 worktree (커밋 대상 아님)
.claude/settings.local.json
.claude/worktrees/
