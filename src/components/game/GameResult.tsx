import house from '../../assets/house.png'

const GameResult = () => {
  return <div className="relative flex h-screen flex-col items-center justify-center bg-center bg-no-repeat" style={{ backgroundImage: `url(${house})` }}></div>
}

export default GameResult
